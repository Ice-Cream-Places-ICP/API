const userTypes = {
    ADMIN: 'admin', 
    DEFAULT: 'default', 
    GUEST: 'guest'
}

const shopRoles = {
    OWNER: 'owner',
    EMPLOYEE: 'employee'
}

module.exports = { 
    userTypes, 
    shopRoles
};